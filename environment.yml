name: dqn-lite
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # for the cu121 torch build use: pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
      - ale-py==0.12.1
      - gymnasium==1.3.0
      - matplotlib==3.11.1
      - moviepy==2.2.1
      - numpy==2.5.2
      - opencv-python==5.0.0.93
      - torch==2.5.1
      - tqdm==4.70.0
